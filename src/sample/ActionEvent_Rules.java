package sample;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;

public class ActionEvent_Rules implements EventHandler<ActionEvent> {
    @Override
    public void handle(ActionEvent actionEvent) {
        // change the window so it displays the rules of the game
    }
}
